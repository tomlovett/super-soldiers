export type Mission = {
  name: string;
};

export type Soldier = {
  first_name: string;
  last_name: string;
  nationality: string;
  gender: string;
  is_alive: boolean;
  user_id: number;
  nickname: string;
  fighter_class: string;
  exp: number;
};
