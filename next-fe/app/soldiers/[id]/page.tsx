import { ReactNode } from 'react'

const SoldierPage = ({ children }: { children: ReactNode }) => <>{children}</>

// Display soldier info; editable
// convert /performance to a separate component reached through active tab

export default SoldierPage
